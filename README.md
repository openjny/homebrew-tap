# Homebrew Tap

Homebrew formulae for openjny's tools.

## Installation

```bash
brew tap openjny/tap
brew install copilot-council
```

## Available Formulae

- `copilot-council` - AI Model Council Pattern CLI that queries multiple AI models and aggregates responses
